if(NOT TARGET zimpl)
  include("${CMAKE_CURRENT_LIST_DIR}/zimpl-targets.cmake")
endif()

set(ZIMPL_LIBRARIES zimpl)
set(ZIMPL_PIC_LIBRARIES zimpl-pic)
set(ZIMPL_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(ZIMPL_FOUND TRUE)

